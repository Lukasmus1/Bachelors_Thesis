namespace User.Models
{
    /// <summary>
    /// Increase the alignment of cops by X points (the enum values).
    /// </summary>
    public enum Alignment
    {
        CopsFirstChoice = 10,
        AIFirstChoice = -10,
        AIDesktopFile = 5,
        QuestionAI = -10,
        QuestionAIHate = 10
    }
}